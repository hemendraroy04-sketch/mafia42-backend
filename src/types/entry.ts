export interface RPEntry {
  rank: number;
  playerId: string;
  playerName: string;
  rp: number;
}

export interface FameEntry {
  rank: number;
  playerId: string;
  playerName: string;
  fame: number;
}

export interface GuildEntry {
  rank: number;
  guildId: string;
  guildName: string;
  gp: number;
}