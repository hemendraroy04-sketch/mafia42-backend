export interface RPEntry {
  rank: number;
  playerName: string;
  rp: number;
}

export interface FameEntry {
  rank: number;
  playerName: string;
  fame: number;
}

export interface GuildEntry {
  rank: number;
  guildName: string;
  gp: number;
}